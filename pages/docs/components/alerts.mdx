# Alert Components

import React from "react";
import ExampleComponent from '../../../components/shared/ExampleComponent.tsx'
import {AlertGeneral, AlertInfo, AlertError} from '../../../database/alerts/alerts.tsx'

### General Alert

import alertGeneralMetaData from '../../../database/alerts/alertGeneralMetaData.ts';

export const AlertsGeneral = () => {
  return (
    <>
    <div className='mb-20'>
        <ExampleComponent codeMetaData={alertGeneralMetaData} componentsArray={[<AlertGeneral />]}/>
    </div>
    </>
  );
};

<AlertsGeneral/>

### Information Alert

import alertInfoMetaData from '../../../database/alerts/alertInfoMetaData.ts';

export const AlertsInfo = () => {
  return (
    <>
    <div className='mb-20'>
        <ExampleComponent codeMetaData={alertInfoMetaData} componentsArray={[<AlertInfo />]}/>
    </div>
    </>
  );
};

<AlertsInfo/>

### Error Alert

import alertErrorMetaData from '../../../database/alerts/alertErrorMetaData.ts';

export const AlertsError = () => {
  return (
    <>
    <div className='mb-20'>
        <ExampleComponent codeMetaData={alertErrorMetaData} componentsArray={[<AlertError />]}/>
    </div>
    </>
  );
};

<AlertsError/>
